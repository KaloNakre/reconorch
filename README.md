# reconorch
Ray + subprocess based orchestrator for CLI security scanning tools (nmap, nikto, gobuster) — parallel &amp; scheduled scans with SQLite result history. For authorized security testing only.
